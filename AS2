#include <stdio.h>
#include <stdlib.h>

int main() {
    char origional_list[500];
    FILE *fp = NULL;
    fp = fopen("../IntegerList.txt", "r");
    fscanf(fp, "%s", origional_list);
    printf("%s", origional_list);
    return 0;
}
